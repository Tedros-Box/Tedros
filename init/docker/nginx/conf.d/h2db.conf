server {
    listen 80;
    server_name h2db.tedros.io;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name h2db.tedros.io;

    ssl_certificate /etc/letsencrypt/live/h2db.tedros.io/fullchain.pem; 
    ssl_certificate_key /etc/letsencrypt/live/h2db.tedros.io/privkey.pem; 

    location / {
        proxy_pass         http://h2db:81;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_buffering    off;
        proxy_cache        off;
    }
}
